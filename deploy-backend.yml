name: Deploy Backend to Render

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  trigger-render-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Trigger Render Deploy (POST)
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
        run: |
          if [ -z "$RENDER_API_KEY" ] || [ -z "$RENDER_SERVICE_ID" ]; then
            echo "Missing RENDER_API_KEY or RENDER_SERVICE_ID. Set repository secrets."
            exit 1
          fi

          echo "Triggering deploy for Render service: $RENDER_SERVICE_ID"
          curl -s -X POST "https://api.render.com/v1/services/${RENDER_SERVICE_ID}/deploys"             -H "Authorization: Bearer ${RENDER_API_KEY}"             -H "Accept: application/json"             -H "Content-Type: application/json"             -d '{"clearCache": true}'             -o /tmp/render-deploy-response.json

          echo "Render response:"
          cat /tmp/render-deploy-response.json
